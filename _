import Foundation
import SwiftKuery
func removeTabelas() {
let utils = CommonUtils.sharedInstance
for tabela in [
MusicasDosAlbuns(),
Musicas(),
Albuns(),
Estilos(),
Bandas()
]{
print(“Apagando \(tabela.nameInQuery)”)
